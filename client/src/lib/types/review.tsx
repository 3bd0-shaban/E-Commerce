import { imageType } from "@lib/types/featuresType";

export interface reviewType {
    _id?: string;
    comment?: string;
    name?: string;
    rating?: string;
    time?: string;
    user?: string;
}