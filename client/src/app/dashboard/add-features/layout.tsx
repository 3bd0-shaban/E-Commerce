import React from 'react'
export const metadata = {
    title: "Add Features Dashboard",
};
export default function Layout({
    children,
}: {
    children: React.ReactNode;
}) {
  return children
}
